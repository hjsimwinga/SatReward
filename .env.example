DATABASE_URL="file:./prod.db"
PORT=30001
NODE_ENV=production

# Blink Wallet API — copy values from your Blink dashboard (never commit real keys)
BLINK_API_KEY=
BLINK_USD_WALLET_ID=
BLINK_USE_STABLESATS=true

# Fixed sats sent as daily reward
REWARD_SATS=500

# Optional
# PAYMENT_POLL_TIMEOUT_SEC=120
